from to_do import TODO


def task9():
    return """
    input insert the card and enter the pin
    check if 
    the card number and pin is validated 
    if both is correct then show the account details
    then enter the withdrawal amount 
    withdrawal amount <= to balance amount in account
    output the amount and display balance = Actual Balance-withdrawn money
    else print insufficient balance cannot withdraw
    """
